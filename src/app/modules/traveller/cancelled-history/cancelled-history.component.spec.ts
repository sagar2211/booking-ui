import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CancelledHistoryComponent } from './cancelled-history.component';

describe('CancelledHistoryComponent', () => {
  let component: CancelledHistoryComponent;
  let fixture: ComponentFixture<CancelledHistoryComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [ CancelledHistoryComponent ]
  //   })
  //   .compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(CancelledHistoryComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
