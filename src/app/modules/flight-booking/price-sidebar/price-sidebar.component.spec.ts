import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PriceSidebarComponent } from './price-sidebar.component';

describe('PriceSidebarComponent', () => {
  let component: PriceSidebarComponent;
  let fixture: ComponentFixture<PriceSidebarComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [ PriceSidebarComponent ]
  //   })
  //   .compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(PriceSidebarComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
