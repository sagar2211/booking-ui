import { ComponentFixture, TestBed } from '@angular/core/testing';

import { OneWayRoundTripComponent } from './one-way-round-trip.component';

describe('OneWayRoundTripComponent', () => {
  let component: OneWayRoundTripComponent;
  let fixture: ComponentFixture<OneWayRoundTripComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [ OneWayRoundTripComponent ]
  //   })
  //   .compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(OneWayRoundTripComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
